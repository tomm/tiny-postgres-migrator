{
  "name": "tiny-postgres-migrator",
  "version": "2.0.0",
  "description": "Simple database schema migrator for the postgres node driver",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tom Morton",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tomm/tiny-postgres-migrator.git"
  },
  "files": [
    "migrations/*.js",
    "src/**/*.js",
    "src/**/*.d.ts",
    "package.json",
    "README.md"
  ],
  "dependencies": {
    "glob": "^7.1.6",
    "postgres": "^3.0.0"
  }
}
